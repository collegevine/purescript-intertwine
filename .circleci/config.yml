version: 2.1

jobs:
  build:
    working_directory: ~/cv

    docker:
    - image: collegevine/rails-build

    parallelism: 1

    steps:
    - checkout: { path: "~/cv" }

    - restore_cache:
        keys:
          - intertwine-{{ .Branch }}-{{ checksum "package-lock.json" }}-{{ checksum "bower.json" }}
          - intertwine-{{ .Branch }}-

    - run: npm install && sysconfcpus -n 2 npm run build && sysconfcpus -n 2 npm run test

    - save_cache:
        key: intertwine-{{ .Branch }}-{{ checksum "package-lock.json" }}-{{ checksum "bower.json" }}
        paths:
          - bower_components
          - node_modules
          - output
          - ~/.cache
